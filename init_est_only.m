% This script is for passing estimation routine over batch.  Rather than give the user options as in init.m, this script only runs the estimation routine.

% Load parameters
params

% Run estimation
calibration_noprod(pop, {});
